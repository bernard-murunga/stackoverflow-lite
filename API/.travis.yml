language: python
python:  
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python -m pytest API/tests/test_endpoints.py
  - python -m pytest --cov=API API/tests
# Coveralls to show test coverage of the modules
after_success:
  - coveralls
